/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['avatars.githubusercontent.com', 'https://nlw-spacetime-server-vk8i.onrender.com'],
  },
}

module.exports = nextConfig
